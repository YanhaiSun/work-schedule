version: '3.8'

services:
  work-schedule:
    build: .
    container_name: work-schedule-app
    ports:
      - "3001:3000"  # 修改端口映射，避免冲突
    environment:
      - NODE_ENV=production
    restart: unless-stopped
