version: '3.8'

services:
  work-schedule:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      # 如果需要持久化数据，可以挂载卷
      # - ./data:/app/data
    restart: unless-stopped